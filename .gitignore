/node_modules/
# Keep dist/ and assets/ for GitHub Pages deployment
# (assets/ is copied from dist/assets/ by the build workflow)

# Environment variables
.env

# Intermediate JSON files (exclude all JSON files in data/ except guide_data.json)
data/*.json
!data/guide_data.json

# Old JavaScript files (migrated to TypeScript)
js/*.js
scripts/*.js

dist

# Ignore root-level assets but allow public/assets/
/assets
!public/assets/

# Git worktrees
.worktrees/